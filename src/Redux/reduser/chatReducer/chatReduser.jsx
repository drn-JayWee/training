
const initialState = {
	chats: [
		{
			id:1,
			name: 'Jack'
		},
		{
			id:2,
			name: 'Eva'
		}
	]
}

export const chatsReducer = (state = initialState, action) => {
	switch(action.type) {

		default:
				return state
	}
}